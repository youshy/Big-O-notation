// Data Structure is a collection of values
// Every structure is specialized in some sort

// Cache - very fast memory, loaded from RAM

// JavaScript doesn't really have integers, it uses 64-bit floats - so, it has maximum biggest value
